[package]
name = "mctl"
version = "0.1.0"
edition = "2024"

[dependencies]
mpris = "^2.1.0"
serde = { version = "^1.0", features = ["derive"]}
serde_json = "^1.0"
clap = {version = "^4.5", features = ["derive", "std"]}

[profile.release]
strip = true
lto = true
opt-level = "z"